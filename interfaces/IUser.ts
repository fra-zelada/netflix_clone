export interface IUser {
    id ?: string;
    name ?: string;
    image ?: string;
    email ?: string;
    emailVerified ?: string;
    createdAt ?: string;
    updatedAt ?: string;
    favoriteIds ?: string[];
}